---
name: owner-dashboard-project
description: "Goal — GCP-hosted owner dashboard over Bitrix24 for monitoring projects & routine tasks, plus actions"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8f5adb8c-2793-4aff-b72d-721e5c27b26d
---

User is the business owner of a marketing agency (portal https://leademy.bitrix24.ru). Goal: build their own web dashboard to monitor agency projects and the active routine tasks per service (SEO / PPC) and per project, and to perform actions (deploy a month's tasks by service/package, create ad-hoc campaign tasks).

Decisions made (2026-06-10):
- Scope: monitoring + actions (not read-only).
- Approach: write plan/ТЗ first, no code yet. ТЗ lives at docs/dashboard-tz.md.
- Hosting: GCP — Cloud Run (Node.js backend extending server.js) + Firebase Hosting (static UI) + Secret Manager for the Bitrix webhook.
- Independence level 1: own UI/logic on top of Bitrix; Bitrix stays the system of record (no own datastore yet).

Added requirement (2026-06-10): from **July 2026** this external service also becomes the **scheduler** that auto-creates monthly routine tasks for active projects (Cloud Scheduler → Cloud Run /api/cron/launch-month, idempotent, Firestore subscription registry). Cutover = 1 July 2026. CRITICAL: an existing Bitrix recurring-task template/robot currently creates the monthly parents (1st of month ~00:03, creator user 1 = Равиль Шакиров the owner); it MUST be disabled before the external scheduler is enabled or projects get duplicate tasks. July 2026 parents do not exist yet as of 2026-06-10 (clean cutover window). Tight timeline (~3 weeks); fallback = keep Bitrix automation for July, cutover 1 Aug.

Also in scope (2026-06-10): dashboard shows finances (budgets/prices/deal sums). Separate T-Bank integration: on incoming payment matched by payer INN, auto-move the deal from stage NEW ("Получение оплаты от клиента") to EXECUTING ("В работе") in pipeline cat 0. Match path: parse invoice number from payment purpose, format = {CLIENT_ID=COMPANY_ID}-{SELF_ID=DEAL_ID}-{MM}-{YYYY} (confirmed via 115-1119-06-2026 → company 115 РЕЛАКС, deal 1119) → crm.deal.get(SELF_ID) verify CATEGORY_ID=0 & STAGE_ID=NEW & COMPANY_ID==CLIENT_ID & payer INN==company RQ_INN → crm.deal.update STAGE_ID=EXECUTING. Fallback if unparsable: payer INN → company → single NEW deal in cat 0. Invoices = smart process entityTypeId 31, linked to deal via parentId2; printed number NOT stored in any field. Exclude own INN 7203566588 (ООО ЛИДЕМИ). T-Bank = T-Business OpenAPI (base https://business.tbank.ru/openapi/api/, Bearer token in Secret Manager), events via webhook on incoming credit (+ statement polling as backup). Partial payment (amount < deal OPPORTUNITY) → do NOT move stage, only on full payment. Idempotency by operationId. Subcontract projects: boolean deal field UF_CRM_SUBCONTRACT (id 1403, created 2026-06-10) — such deals auto-move NEW→EXECUTING WITHOUT payment (T-Bank matcher skips them); transition done ONLY at month-start launch (no periodic check). Responsibles finalized: SEO & Администрирование(149)→101 Максим Логвинов; PPC parent→17 Святослав Павлишин; PPC subtasks→103 Владислав Павлишин +accomplice 17. Global auto-task deadline = +1 month +3 RF working days (isdayoff.ru calendar). Service 149 "Поддержка"→ only a parent task "<Project> — Администрирование сайта (Месяц Год)", no subtasks; only 139/141/149 generate tasks.

Update (2026-06-17): уточнения архитектуры. (1) Уже НЕ полностью stateless — добавлен **Firestore** для того, чего нет в Bitrix: HR/ручные данные (рубрика вовлечённости, ownership-владельцы, нормативы по типам задач), маппинги (documentId, имя→user_id, prospectDealId→projectGroupId), дедуп (operationId Т-Банка, cron). Операционные данные по-прежнему читаются живьём. (2) Добавлен **аналитический модуль метрик эффективности сотрудников** — docs/dashboard-tz.md §10, регламент docs/upravlenie-effektivnostyu.md (см. [[staff-effectiveness-reglament]]). (3) **UI-структура** — docs/dashboard-ui-struktura.md; дизайн-код Leademy (светлая тема, шрифт Google Sans Cyr, холодные нейтрали для фона/сайдбара, белая рабочая область, бренд-синий #14274e), desktop-first. (4) **Уведомления владельцу** — личным сообщением в Bitrix (im.message.add); scope `im` выдан 2026-06-17. (5) Отдельный воркстрим автоматизации конспектов встреч — см. [[meeting-conspect-automation]].

Update (2026-06-19): GCP-доступ для b24-cloud-mcp настроен. Проект `midyear-reactor-441919-p4` (аккаунт marketing@d-gtl.com; вероятно общий/маркетинговый — возможно стоит выделенный проект под дашборд), задан в .env как `GOOGLE_CLOUD_PROJECT`. SA `b24-dashboard@midyear-reactor-441919-p4.iam.gserviceaccount.com` создан, роли `roles/storage.objectViewer` + кастомная `b24CloudReader` (storage.buckets.list/get). **Ключи SA запрещены оргполитикой** `constraints/iam.disableServiceAccountKeyCreation` → используется **keyless impersonation** (`gcloud auth application-default login --impersonate-service-account=...`); `GOOGLE_APPLICATION_CREDENTIALS` намеренно не задан; в проде SA привязывать к Cloud Run/VM. gcloud SDK 564 установлен глобально. MCP проверен — listBuckets от имени SA отдаёт 13 бакетов.

Update (2026-06-21): начата ВЁРСТКА фронтенда. Подключён Figma MCP (`figma-developer-mcp`, в `.mcp.json`, ключ `FIGMA_API_KEY` в `.env`; в `.vscode/mcp.json` через prompt). Дизайн Figma `wgYIO9vRthxdr2MhSzIxJg` нода `1:2` = 3 экрана (screen-overview/project/employees) + тултипы; покрывает 3 из 7 разделов нав-меню (нет макетов: Задачи, Финансы, Подписки, Планировщик + модалки «Развернуть месяц»/«Внеплановая задача»). Свёрстан статический SPA в **`web/`**: `index.html` (сайдбар+рабочая область), `styles.css` (токены Leademy), `app.js` (хэш-роутер, грузит `web/screens/<name>.html` партиалами), 7 экранов в `web/screens/` (3 по Figma + 4 по ТЗ на тех же компонентах). Демо-данные, без API-привязки. Превью: `python3 -m http.server --directory web` (или `.claude/launch.json` сервер `dashboard`, порт 4174). Добавить страницу = новый `screens/<name>.html` + пункт в `#nav` + имя в массиве `SCREENS` в app.js. Детали — docs/dashboard-ui-struktura.md §«Реализация».

Update (2026-06-24): решение по хостингу — Cloud Run-сервис разворачиваем В СУЩЕСТВУЮЩЕМ проекте `midyear-reactor-441919-p4` (НЕ заводим отдельный GCP-проект — прежнее «возможно выделенный проект» отменено). Подготовлены артефакты деплоя: `dashboard/server.js` (каркас бэкенда — /healthz, /api/version, раздача SPA из web/, заглушки 501 на /api/cron/launch-month|launch-weekly + /api/tbank/webhook), корневой `Dockerfile` (node:20-slim, CMD node dashboard/server.js, PORT 8080), `.dockerignore`, `.gcloudignore`, `scripts/deploy-dashboard.sh` (идемпотентный: enable APIs, секрет B24_WEBHOOK_BASE из .env, `gcloud run deploy --source` от имени SA b24-dashboard@, region europe-west1, --allow-unauthenticated пока каркас). npm: `start:dashboard`, `deploy:dashboard`. Локальный smoke-тест ОК. Деплой НЕ запущен: gcloud-токен протух (нужен интерактивный `gcloud auth login`), ADC отсутствует. ВАЖНО до прод-данных: убрать --allow-unauthenticated (поставить IAP/identity) — сейчас открыто.

Update (2026-06-24, деплой выполнен): сервис **leademy-dashboard** РАЗВЁРНУТ в Cloud Run, проект midyear-reactor-441919-p4, регион europe-west1. URL: https://leademy-dashboard-940270882599.europe-west1.run.app (ревизия 00003). Деплой через `npm run deploy:dashboard`. Грабли по ходу: (1) runtime-SA b24-dashboard@ требовал `roles/secretmanager.secretAccessor` на секрет B24_WEBHOOK_BASE — выдан на уровне секрета; (2) путь **/healthz зарезервирован Google Front End** (до контейнера не доходит, отдаёт платформенный 404) → health перенесён на **/health и /api/health**.

Update (2026-06-24, аутентификация СДЕЛАНА, ревизия 00004): серверный логин. `dashboard/auth.js` — scrypt-проверка пароля + HMAC-подписанная HttpOnly/Secure/SameSite=Lax cookie `leademy_session` (TTL 12ч), без новых зависимостей (node:crypto). Эндпоинты `POST /api/login`, `/api/logout`, `GET /api/me`; мидлвара `requireAuth` закрывает всё кроме `/api/login`, `/api/logout`, `/health`, `/api/health`, `/login.html`, `/styles.css` и машинных `/api/cron/*`+`/api/tbank/webhook` (у них будет своя auth — OIDC/подпись). Логин `admin-ravil` = env `DASHBOARD_USERNAME`; секреты `DASHBOARD_PASSWORD_HASH` (scrypt) и `SESSION_SECRET` в Secret Manager (provision: `scripts/provision-auth.sh`). `web/login.html` теперь POST на /api/login (не localStorage). На Cloud Run `--allow-unauthenticated` ОСТАВЛЕН намеренно — гейт делает приложение. End-to-end на проде ОК: no-auth→401/302, верный→cookie→200, неверный→401. Пароль admin — в `.secrets/dashboard-admin.txt` (gitignored).

Update (2026-06-24, ЖИВЫЕ ДАННЫЕ — первый срез): API дашборда поверх Bitrix. `dashboard/bitrix24.js` — клиент REST (b24Call/b24Total/b24ListAll с пагинацией, база из B24_WEBHOOK_BASE). `dashboard/api.js` — Router (TTL-кэш 30с) за гейтом: `GET /api/projects` (рабочие группы, исключая внутренние по regex /leademy|тест|test/) и `GET /api/overview` (KPI + матрица услуга×состояние + лента). Подключён экран «Обзор»: `web/overview.js` грузит /api/overview по событию screen:render; `web/screens/overview.html` переведён на якоря-id; `web/app.js` добавил `window.apiGet` (401→login) и логаут через POST /api/logout; убран старый localStorage-гейт из index.html. Классификация услуги по тексту задачи (SEO/PPC/Поддержка/Прочее), состояние по deadline+status (2/6=ожидает,3=актив,<now=просрочено). Проверено на живом портале: activeProjects=51, servicesInWork(активные recurring)=9, задач незакрытых 45 (почти все в статусе «ждёт»), просрочено 4, donePct 67%. activeProjects теперь ТОЧНЫЙ (ревизия 00006): = уникальные группы среди базовых сделок активных recurring через `UF_CRM_1727554217` (deal→group, поле заполнено у всех 9) → =9. Бонусом из тех же сделок взят `OPPORTUNITY` → KPI `monthlyBudget` (сумма активных месячных подписок = 309 000 ₽, плитка «Бюджеты/мес»). Связь: recurring.DEAL_ID → crm.deal.list(@ID) → UF_CRM_1727554217 + OPPORTUNITY. Финансы по ОПЛАТАМ (смарт-счета DT31_9:P) — всё ещё TODO.

Update (2026-06-24, РЕДИЗАЙН под Figma): фронт приведён к макету Figma `wgYIO9vRthxdr2MhSzIxJg` node 1:3 (screen-overview). Дизайн-система переписана в `web/styles.css`: шрифты Instrument Sans + Inter (Cyrillic-фолбэк) + `--font-brand` Google Sans Flex→Inter (lowercase бренд); токены #14274E navy/#1A356B selector/#ECEFF4 sidebar/#F5F6F9 tiles/#EF4444 red/#999 muted; глобальный navy-аппбар (64px) с заголовком+селектором месяца+колокольчик+аватар; сайдбар 220px с Lucide-иконками (inline SVG, не эмодзи); KPI-сетка 3×2 плитки; таблица .table-card с navy-шапкой (классы th/td .c center/.r right/.total/.bad); правая «Лента событий» 260px карточками. `web/index.html` перестроен (сайдбар+аппбар+main), логаут перенесён на клик по аватару (#logout=img). Логотип/аватар скачаны из Figma в `web/assets/{logo,avatar}.png`. `web/screens/overview.html` = .page-body(.center-content + aside.events). `web/overview.js` обновлён под новую разметку + пишет месяц в #appMonth. login.html бренд = картинка-лого. Брейкпоинты: <1180 прячет ленту, <1024 KPI в 2 колонки. Проверено в preview (1440px) — совпадает с макетом, прочие экраны (на .panel/.table/.toolbar) не сломаны. Локальный preview-конфиг: `.claude/launch.json` сервис `dashboard-app` (порт 8095, тестовые auth-секреты inline, пароль preview123).

Update (2026-06-24, экран ПРОЕКТЫ по Figma + живые данные): Figma node screen-project=1:148 (детальная карточка проекта, НЕ список). Реализован `web/screens/project.html` + `web/project.js` + стили в styles.css: шапка с хлебной крошкой/H1/статус-чипом, **селектор проекта** (dropdown), услуги-аккордеон «Услуги месяца» (прогресс done/total, задачи со статус-иконками done/wait/late + бейджи Просрочено/ждёт), блок «Финансы проекта» (Бюджет/Сумма сделки = OPPORTUNITY, Оплачено=— TODO), правый сайдбар «Другие проекты клиента» + «История платежей»(TODO). API: `GET /api/project/:id` — услуги (tasks.task.list filter GROUP_ID + >=CREATED_DATE monthStart, классификация по тексту), бюджет (recurring deal→OPPORTUNITY по UF_CRM_1727554217), otherProjects (др. подписочные группы). `/api/projects` дополнен флагом `subscribed` + сортировкой (подписочные первыми) → дефолт открывает проект с данными. Проверено в preview 1440px — совпадает с макетом. Figma nodes: screen-employees=1:316/content 1:350. Аватары проекта/сотрудников в Figma — imageRef (не качал, юзаю web/assets/avatar.png).

Update (2026-06-24, ЭКРАН СОТРУДНИКИ — БЛОКЕР данных): Figma 1:350 = grid 2×2 карточек team-member с метриками Задач/Срок/Стандарт/MARK/Возврат + Направления(SEO/PPC/SUP dots) + Динамика 3 мес (бары) + Вовлечённость(Апр/Май/Июн) + Proactive/Own чипы + секция «Точки роста» (таблица рекомендаций). ПРОБЛЕМА: из Bitrix живьём берутся только «Задач»(count) и «Срок»(on-time% по deadline); Стандарт/MARK/Возврат/Вовлечённость/Proactive/Own/Точки-роста = ручные HR-данные → нужен Firestore HR-слой (см. [[staff-effectiveness-reglament]]), которого ещё нет. Поэтому экран Сотрудники НЕ сделан вживую — ждёт Firestore. metrics.js уже содержит чистые расчёты (проектов/чел, задач/нед, час отписки) на демо-данных.

**Why:** owner wants to stop depending on the Bitrix24 UI and own the scheduling logic externally.
**How to apply:** reuse b24Call/toFormPairs from server.js (extract to src/bitrix24.ts); routine-task templates and service→task mapping already documented — see [[b24-routine-task-model]]. Full spec: docs/dashboard-tz.md.
